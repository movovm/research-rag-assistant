name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
          cache: maven
      - name: Verify with MySQL Testcontainers
        run: mvn --batch-mode verify
      - name: Reject skipped tests
        shell: bash
        run: |
          if grep -RE 'skipped="[1-9][0-9]*"|<skipped[ />]' target/surefire-reports/TEST-*.xml; then
            echo 'Skipped tests are not allowed in CI'
            exit 1
          fi
      - name: Check committed secrets
        shell: bash
        run: |
          if git grep -nE '(sk-[A-Za-z0-9_-]{20,}|pcsk_[A-Za-z0-9_-]{20,}|api[-_]?key:[[:space:]]*"?[A-Za-z0-9_-]{8,})' -- ':!docs/**' ':!*.example'; then
            echo 'Potential secret found'
            exit 1
          fi
      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: jacoco-report
          path: target/site/jacoco
