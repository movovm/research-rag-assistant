# Demo Script

1. 启动应用并打开 `http://localhost:8080`，确认 3 个内置文档已索引。
2. 提问“Redis 缓存穿透如何处理？”，观察流式回答与右侧 Top-K 证据。
3. 继续问“这个方案有什么风险？”，确认 Query Rewrite 补全上轮主题。
4. 打开“检索调试”，搜索 `Redis 缓存穿透`，比较 BM25、Dense 与融合分数。
5. 打开长期记忆，查看默认“先给结论，再给步骤”的主动偏好。
6. 上传一份不含敏感信息的 Markdown 或 PDF，检查文档列表和语义块数量变化。
7. 清空会话，确认短期记忆清除但知识库与长期记忆仍保留。
